! function(a) {
    a.fn.SuperBox = function(b) {
        var c = a('<div class="superbox-show"></div>'),
            d = a('<img src="" class="superbox-current-img"><div id="imgInfoBox" class="superbox-imageinfo inline-block"> <h1>Image Title</h1><p class="age_sex"></p><p class="address"></p><br/><p><a href="" class="btn btn-primary btn-sm details">Details</a></p> </div>'),
            e = a('<div class="superbox-close txt-color-white"><i class="fa fa-times fa-lg"></i></div>');
        c.append(d).append(e);
        a(".superbox-imageinfo");
        return this.each(function() {
            a(".superbox-list").click(function() {
                $this = a(this);
                var b = $this.find(".superbox-img"),
                    e = b.data("img"),
                    age = b.data("age"),
                    sex = b.data("sex"),
                    address = b.data("address"),
                    url = "/model_show/"+b.data("id")+"/"
                    f = b.attr("alt") || "No description",
                    g = e,
                    h = b.attr("title") || "No Title";
                d.attr("src", e), a(".superbox-list").removeClass("active"), $this.addClass("active"), d.find("em").text(g), d.find(">:first-child").text(h),d.find(".age_sex").html(age+" , "+sex),d.find(".address").html(address),d.find('.details').attr('href',url), 0 == a(".superbox-current-img").css("opacity") && a(".superbox-current-img").animate({
                    "opacity": 1
                }), a(this).next().hasClass("superbox-show") ? (c.is(":visible") && a(".superbox-list").removeClass("active"), c.toggle()) : (c.insertAfter(this).css("display", "block"), $this.addClass("active")), a("html, body").animate({
                    "scrollTop": c.position().top - b.width()
                }, "medium")
            }), a(".superbox").on("click", ".superbox-close", function() {
                a(".superbox-list").removeClass("active"), a(".superbox-current-img").animate({
                    "opacity": 0
                }, 200, function() {
                    a(".superbox-show").slideUp()
                })
            })
        })
    }
}(jQuery);